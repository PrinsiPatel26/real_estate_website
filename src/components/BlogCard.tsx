import React from 'react';
import { Link } from 'react-router-dom';
import { ArrowUpRightIcon } from 'lucide-react';
import type { BlogArticle } from '../data/blogs';

export function BlogCard({ article }: { article: BlogArticle }) {
  return <article className="group flex h-full flex-col border border-black/12 bg-[#f7f5f0] transition duration-300 hover:-translate-y-1 hover:border-[#c9a227]/60 hover:shadow-[0_16px_34px_rgba(17,17,17,0.1)]"><Link to={`/blog/${article.slug}`} className="block overflow-hidden"><img src={article.image} alt={article.title} loading="lazy" className="aspect-[16/10] w-full object-cover transition-transform duration-500 group-hover:scale-[1.03]" /></Link><div className="flex flex-1 flex-col p-6 sm:p-7"><p className="text-[0.68rem] font-semibold uppercase tracking-[0.16em] text-[#c9a227]">{article.category}</p><p className="mt-2 text-[0.7rem] uppercase tracking-[0.1em] text-[#78736a]">{article.date} <span className="px-1 text-[#c9a227]">|</span> {article.readTime}</p><h2 className="mt-5 font-display text-[1.45rem] font-medium leading-[1.16] text-[#151515] sm:text-[1.65rem]"><Link to={`/blog/${article.slug}`} className="transition-colors hover:text-[#c9a227]">{article.title}</Link></h2><p className="mt-4 text-[0.95rem] leading-relaxed text-[#4e4b45]">{article.excerpt}</p><Link to={`/blog/${article.slug}`} className="mt-auto inline-flex items-center gap-2 pt-7 text-[0.68rem] font-semibold uppercase tracking-[0.16em] text-[#151515] transition-colors hover:text-[#c9a227]">Read Article <ArrowUpRightIcon className="h-4 w-4" aria-hidden="true" /></Link></div></article>;
}
